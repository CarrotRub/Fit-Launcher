export type GameDetails = {
  tags: string;
  companies: string;
  language: string;
  originalSize: string;
  repackSize: string;
};

export type GamePageState = {
  gameHref: string;
  gameTitle: string;
  filePath: string;
};
